VITE_API_URL=http://localhost:8000

# Optional — leave empty in dev. Get the DSN from sentry.io after creating
# a "React" project. Errors are forwarded to Sentry only when this is set.
VITE_SENTRY_DSN=
