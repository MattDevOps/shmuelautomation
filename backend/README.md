# shmuel-backend

FastAPI backend. See the repo-root `README.md` for the full dev setup.

## Quick commands

```bash
uv sync                              # install deps
uv run uvicorn shmuel_backend.main:app --reload   # dev server on :8000
uv run pytest                        # unit tests
uv run ruff check .                  # lint
```
